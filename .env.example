# Hotel Price Scraper — Environment Variables
# Copy this file to .env and fill in values if needed.

# Headless mode: set to "false" to watch the browser (useful for debugging)
HEADLESS=true

# Request timeout in milliseconds (default: 60000)
TIMEOUT=60000
